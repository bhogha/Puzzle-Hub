// ── obj_wordle — Create ───────────────────────────────────────────────────────
// Phase 0 scaffold. Real setup (puzzle load, grid/keyboard layout, hint flow,
// review mode) is built in Phases 1–5. For now this just establishes the screen
// so the Wordle card opens a valid, navigable room.

accent = PH_COL_GREEN;
