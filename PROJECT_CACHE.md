# Puzzle Hub — Project Cache (for Claude)

_Internal reference map of the codebase, written to make future work faster. Not player- or design-facing — for design rules see the design docs in `../Daily Puzzle/Docs/` (`GDD.md` is the source of truth for rules; per-puzzle plans in `*_PLAN.md`, `MISSIONS_PLAN.md`). Last synced: 2026-06-11 against v0.3. **2026-06-17 (image buttons + Daily Spin redesign): (1) NEW shared image-button system — `ph_draw_btn_bg(spr,x1,y1,x2,y2)` (`scr_draw`) draws a button-background PNG as a horizontal 3-slice (uniform-scaled round-corner caps + stretched flat middle; baked drop shadow stays put) so one source covers any width; source frame shared by all bg sprites = 230px tall (20px margin, 149px body, ~10px shadow), loaded **origin top-left**. `ph_btn_sprite_for(body_col)` maps a button's BODY colour → its art sprite, so `ph_draw_reward_btn`/`ph_draw_nav_btn` now route blue→`spr_btn_blue`, green→`spr_btn_green`, pink→`spr_btn_pink`, give-up-red `rgb(235,90,90)`→`spr_btn_red` automatically (unknown colour falls back to the old primitive chip). This re-skins EVERY reward/nav button app-wide (win CLAIM/DOUBLE/SHARE/HOME/NEXT GAME/YESTERDAY, Level-Up, hint BUY/FREE, Wordle lose BUY/FREE/GIVE UP/CANCEL, Event-Hub CLAIM/COLLECT, Daily Spin CLAIM/DOUBLE) with no call-site changes. Sprites added in `obj_persistent` from `datafiles/icons` (`blue_button_xlarge.png`, `green_button_large.png`, `share_button.png`, `giveup_button.png`, `share_icon.png` — all NEW IncludedFiles in the `.yyp`). SHARE button now draws the `share_icon.png` glyph (falls back to `ph_draw_share_glyph`). (2) Daily Spin (`scr_spin`) re-skinned to the Penpot "Daily Spin Modal": dropped the full dark scrim for a **cream bottom-sheet** (`PH_COL_YELLOW_SOFT`, rounded top, slides up — the "hint-modal yellow box" the design reuses) covering the lower ~55% with the hub still visible above; wheel sits at the sheet top (sized to fit the room above the text block). Prize **numbers are now angled radially per slice** via `draw_text_transformed` (rotation = slice radial dir folded into [-90,90] so all stay readable; top/bottom read vertical, sides fan ±30°) with the **coin tucked inward** (0.40R) of the number (0.62R). Pointer is now solid design-red `#f43327`. Idle title moved into the sheet ("Spin and Win!" dark + subtitle ink, both readable on cream). Claim block restyled: "Claim your reward!" + a **dark** (was yellow) amount + coin with comfortable spacing, CLAIM | DOUBLE on the new blue button art. `ph_spin__draw_result(_s,_cx,_slide)` signature changed (was `_result_top,_btn_cy`).** **2026-06-16 (Colordoku — 11th puzzle, "Queens / Meowdoku" colour-region logic): new `scr_colordoku` + `obj_colordoku` + `rm_colordoku`, 6×6 colour-region board where one teal-gem queen goes per row/column/region with no two touching (incl. diagonally — the LinkedIn-Queens *touch* rule, not full-diagonal). Single tap CYCLES a cell empty→X→queen→empty (no double-tap/timing — the original tap/double-tap scheme tested poorly on touch, replaced 2026-06-16); conflicting queens flash red-orange (Sudoku-style, no block). HINT = auto-fill EVERY blank cell forced to X by current queens (one click; `cd_apply_hint`/`ph_colordoku_forced_x`). No bonus, no loss. **Daily-goal model (decided 2026-06-16): the goal is "any `PH_PUZZLES_PER_DAY` (10) solves out of ALL available puzzles" — now 11 incl. Colordoku, future 10-of-15 etc.** Colordoku counts toward the count/gift/streak/perfect-day like every other puzzle (NO skip rule — the earlier COLORDOKU skip was removed from `scr_save`). The hub progress display caps at the goal: `_solved_today = min(ph_solved_count_on(...), PH_PUZZLES_PER_DAY)` in `obj_hub/Draw_64` (so 11 solves still shows 10/10). Perfect-day mission uses the existing `_c == PH_PUZZLES_PER_DAY` crossing detector (fires once when the 10th solve lands; an 11th doesn't double-count). `ph_puzzle_is_solved` (scr_economy) has an `rm_colordoku` case so NEXT-GAME nav + all-solved detection include it. Accent `PH_COL_BRTEAL` #5af2bc / `PH_COL_BRTEAL_DEEP` title. Pool = 60 unique boards (30 past+30 future) from `tools/gen_colordoku.py` (queen permutation → grown contiguous regions → solver-verified unique). Assets `card_brightteal.png` + `game_colordoku.png` (provided). Registered in `.yyp` (obj/rm/scr resources, RoomOrderNodes, 3 IncludedFiles). ⚠️ NEW RESOURCES — register only with GameMaker fully quit, then reopen + Clean rebuild (see `project_puzzle_hub_yyp_clobber` / `_yyc_clean_rebuild`). `PH_PUZZLES_PER_DAY` stays 10 (= the goal, not the card count).** **2026-06-16 (hub calendar + win-nav): (1) Added a hub **month selector** — when the calendar is expanded a mint slider bar below the date grid (`< PrevMonth` / `NextMonth >`, Penpot "Month Slider") steps the VIEWED month via new `cal_view_year`/`cal_view_month` state + `hub_month_step`/`hub_view_to_selected`/`hub_build_month_grid` in `obj_hub/Create_0`; the calbar label now follows the viewed month, backward is unlimited, the next arrow hides at today's real month (no future-only months). New `LAYOUT.cal_monthnav_gap`/`cal_monthnav_h`; open-calendar `_post_cal`/`_teal_bottom` in `obj_hub` Step+Draw now reserve room for the bar; render in Draw §3b. (2) Fixed **NEXT GAME → hub when a day is fully solved**: `ph_puzzle_is_solved` (`scr_economy`) only switched on 6 rooms, so colorlink/wordbend/arrows/ladder never counted as done and `ph_win_next_unsolved_room` never returned "" — added the 4 missing cases so an all-solved day correctly falls back to the hub.** **2026-06-15 (new-player UX pass): (1) Word Bend board 6×6 → 5×5 (`gen_wordbend.py` `N=5`, pool regenerated 30 past + 30 future, 5×5 fallback in `scr_wordbend`); (2) Ladder hint is now TWO levels per rung — 1st highlights the tile, 2nd highlights the correct letter on the keyboard (`hint_lvl`/`hint_key`, persisted via `ph_ladder_save_state` 5th arg; modal subtitle swaps via `ld_hint_subtitle`); (3) Ladder wrong guess now costs +5 s (`PH_LADDER_PENALTY_SECS`, folded into `timer_base_secs`) with a red timer flash + floating "+5s" (`pen_t`); (4) Wordle + Ladder keyboards give press feedback — the tapped key darkens (`merge_color(_,c_black,0.22)`) for `KEY_PRESS_DUR` frames (`key_press_tag`/`key_press_ch`); (5) Arrows hint now recolours the safe arrow GREEN permanently until played (`ar_hinted[]` bool set, persisted in `arrows_state.hinted`, `ar_hint_pick` skips already-hinted) — replaced the old 3 s white pulse that players kept losing.** **2026-06-15: Color Link re-tuned easier — board 12×9 → tall 9×7 (rows×cols), flows 9–10 → 6–7 (smaller grid, fewer colours); regenerated pool + fallback via `gen_colorlink.py` (ROWS/COLS=9/7, FLOWS 6–7, SEG_MAX 13).** **2026-06-11: Shikaku, Color Link, Word Bend and Arrows all standardised to a uniform 9×9 board** (level pools regenerated to 60 boards each = 30 dated from 2026-06-11 + 30 fallback; controllers were already size-driven so only data + size constants/fallbacks changed — see GDD §7 2026-06-11). **2026-06-11 (follow-up): per-puzzle re-tune — Hue Sort 5×5→4×4 (`PH_HUESORT_SIZE`), Word Bend 9×9→6×6 (pool regenerated via the upgraded snakier `gen_wordbend.py`), Color Link 9×9→tall non-square 12×9 (rows×cols); Color Link controller + `scr_colorlink` + `gen_colorlink.py` moved from square `N` to `ROWS`/`COLS` like Arrows, data shape now `rows`/`cols` (legacy `size` still accepted).** Prior sync (incl. Ladder — 10th puzzle, word ladder: seed word + 10 rungs, change one letter/step, tap-select + type-replace, green/red feedback, letters-only keyboard, no loss; Arrows — 9th puzzle, tap-to-clear bent "snake" arrows, tip-lane escape, no loss / +5 s time penalty; Word Bend, Color Link, Hue Sort, Wordle with loss flow, Shikaku, shared hint modal + Level-Up reward screen)._

> Keep this in sync when files move, globals are added, or the boot/save flow changes. If it drifts from the code, the code wins — re-verify before relying on a line here.

---

## 1. What this is

GameMaker (IDE `2026.0.0.16`, runtime `2026.0.0.23` — LTS) mobile daily-puzzle app. Portrait, designed at **1080×1920**. Project file: `Puzzle Hub.yyp` (lives in this folder, which is a sibling of the `Daily Puzzle` reference folder — see auto-memory `project_puzzle_hub_paths`).

Ten puzzles implemented — **Anygram**, **Sudoku**, **Word Wave**, **Shikaku**, **Wordle**, **Hue Sort**, **Color Link** (Flow Free), **Word Bend** (tile-the-board word trace, Elevate-style), **Arrows** (9th; tap-to-clear bent "snake" arrows, *Arrows – Puzzle Escape*-style), **Ladder** (10th; classic word ladder — seed word + 10 rungs, change one letter per step), **Colordoku** (11th, added 2026-06-16; "Queens / Meowdoku" 6×6 colour-region logic — one queen per row/col/region, no two touching). **Daily goal = any `PH_PUZZLES_PER_DAY` (10) solves out of all available puzzles (now 11); every puzzle counts, display caps at 10/10.** Economy = XP / Levels / Coins. Full design rules in `GDD.md`. **Wordle is the only puzzle that can be lost** (out of guesses → consolation XP + "missed" state); see `WORDLE_PLAN.md` for its phased build. **Arrows** has no loss state — a blocked tap costs a +5 s time penalty only; see `ARROWS_PLAN.md`. **Ladder** has no loss state — a wrong letter just flashes red and reverts; see `LADDER_PLAN.md`.

---

## 2. Directory map

```
Puzzle Hub/
├─ Puzzle Hub.yyp              project manifest (resource + room order list)
├─ GDD.md                      design doc — source of truth for rules
├─ README.md                   short public overview
├─ PROJECT_CACHE.md            (this file)
├─ scripts/                    all shared logic (pure functions, ph_ prefix)
├─ objects/                    screens + puzzle controllers (one per screen)
├─ rooms/                      one room per screen (rm_*)
├─ datafiles/                  shipped at working_directory at runtime
│  ├─ puzzles_anygram.json     date-keyed + seed-fallback puzzle pool
│  ├─ puzzles_sudoku.json
│  ├─ puzzles_wordwave.json
│  ├─ icons/                   ~40 PNGs, loaded via sprite_add in obj_persistent
│  └─ fonts/                   Lilita One (display), Nunito (body)
└─ options/                    per-platform export settings
```

`datafiles/` contents are copied to `working_directory` at runtime, so code reads e.g. `working_directory + "icons/..."` and `working_directory + "puzzles_anygram.json"`.

---

## 3. Scripts (the API surface) — `scripts/`

All functions are global, prefixed `ph_`. No structs-with-methods except inline closures in objects.

| Script | LOC | Holds |
|---|---|---|
| `scr_constants` | 107 | All `#macro`s: palette colors, canvas, **economy numbers**, daily schedule indices, save filename, debug flags. Also `ph_game_cards()` (hub card list). |
| `scr_economy` | ~520 | Economy: `ph_level_from_xp`, `ph_xp_in_level`, `ph_grant_xp(save, amt, _auto_coins=true)` (pass `false` to **defer** level-up coins to the Level-Up screen), `ph_grant_coins`, `ph_spend_coins`, `ph_levelup_pending`, `ph_xp_claimed/mark`, share helpers. **Shared Win Screen** (`ph_win_create/step/input/draw/grant/begin_claim/celebrate`). **2026-06-14 redesign:** result line is now a single **"Completed in [time pill]"** below the recap (old "You solved todays \<PUZZLE\> / in …" gone), followed by **"Claim your reward!"** + a large **reward-amount display** (`<amount>` + 3D star, `fnt_num_xl`) above the XP bar; reward buttons carry **word labels** (**CLAIM** / **DOUBLE**+TV) instead of the number. Done-state nav stack (SHARE·HOME / NEXT GAME / YESTERDAY) is now bottom-anchored. Same pattern mirrored on the Wordle lose screen + Level-Up screen. **Win-screen nav shortcuts:** `ph_puzzle_is_solved(room,date)`, `ph_win_next_unsolved_room(date)` (wrap), `ph_win_prev_unsolved_date(room,date)`, `ph_win_go_next`, `ph_win_go_yesterday`, `ph_win_route(room,date)` (detours via `rm_win` when a level-up is pending, stashing `global.post_levelup`). |
| `scr_hint` | ~330 | **Shared hint-acquisition flow** (modal + placeholder rewarded video), struct-based, reused by every puzzle. `ph_hint_create(apply_method, accent, subtitle="", key="")` → struct; `ph_hint_open/tick/input/is_open`; `ph_hint_draw_feedback/modal/video`. `ph_hint_input` returns `"none"/"consumed"/"paid"/"freed"/"poor"`. **2026-06-14:** modal cost as a "100 🪙" row + bare **BUY**/**FREE**+TV labels; generic `ph_video_overlay(timer,delay,accent)` "VIDEO PLAYING" placeholder. **2026-06-19 (hint reveal pass):** (1) **Post-buy reveal** — `apply()` now RETURNS a target `{x,y,r}` (or `{iris:false,frames}` for a puzzle-custom reveal) instead of `true`; on BUY/FREE the flow stores a **deferred result** (`result_pending`) + arms a reveal (`reveal_active/_t/_frames/_iris/_x/_y/_r`; knobs `REVEAL_FR_DEF/REVEAL_R0/REVEAL_WIND`) via `ph_hint__begin_reveal`, and `ph_hint_input` emits `"paid"/"freed"` ONLY after the reveal ends — so the controller's win-check (run on that result) lands AFTER the reveal. `ph_hint_draw_reveal(h)` draws the default **iris** (accent spotlight winds up → `ph_ease_in_out` contracts onto the cell → fades + additive landing flash); `ph_hint_revealing(h)`/`ph_hint_reveal_p(h)` drive custom reveals (Color Link snake). ⚠️ Controllers must poll `ph_hint_input` EVERY frame so the deferred result is caught — Sudoku & Wordle relocated their call above the `if(!pressed)exit;` gate. (2) Modal draws the **coin balance opaque over the dim** + **greys/disables BUY** when `coins < PH_HINT_COST` (FREE still available; old "poor" dismiss gone). (3) `ph_hint_pill_draw(l,t,r,b,shadow)` — draws the standard HINT pill (chip·bulb·"HINT") and **bounces** the whole pill (decaying double-hop, then rest) after `PH_HINT_IDLE_SECS` (5 s) idle, reading `global.ph_idle_anchor` (init+reset on any tap in `obj_persistent`); replaces each puzzle's separate chip+bulb+text pill lines (hit bounds stay at rest so taps are accurate). |
| `scr_save` | ~350 | Load/write/reset save; per-puzzle solved tracking; streak recompute; bonus-word tracking; Sudoku grid + Wordle state persistence; `WORDLE_MISSED` skip in `ph_solved_count_on`; **shared pause/resume play-timer** (`ph_timer_get/set/now/commit/step` over `save.timers["<puzzle>_<date>"]`). **Central data layer.** |
| `scr_dates` | 44 | Date-key formatting (`ph_today_key`, `ph_date_key`), `ph_seed_from_key` (day-index seed), weekday/month math, `ph_date_add_days`. |
| `scr_puzzles` | 484 | Anygram + Word Wave loaders, normalizers, classifiers, solved checks. Both puzzles' pure logic. |
| `scr_sudoku` | 162 | Sudoku loader, normalizer, conflict/row/col/box/all-solved checks, grid serialize. |
| `scr_shikaku` | ~210 | Shikaku loader, normalizer (clues + solution rects), per-rect correctness, full-partition solution check, state serialize/restore, done flag. |
| `scr_colorlink` | ~225 | Color Link (Flow Free) pure logic: JSON loader/cache + two-pass date select, `ph_colorlink_make` (a/b/path → {r,c}), `ph_colorlink_color` (vibrant palette by index), `ph_colorlink_endpoint_color`, `ph_colorlink_is_solved` (all flows connect endpoints + non-overlap + full coverage), `ph_colorlink_longest_unsolved` (hint target), routes serialise/restore, `ph_colorlink_is_done`/`mark_done`. **Tall non-square 9×7 (rows×cols)** board; `ph_colorlink_make` reads `rows`/`cols` (legacy square `size` still accepted) and all index math uses `cols` (row-major `r*cols+c`); fallback 9×7 7-flow board. `ph_colorlink_color` palette has **10 distinct hues** (boards use 6–7 flows, well within it w/o reuse). |
| `scr_huesort` | ~270 | Hue Sort pure logic: JSON loader/cache, hex⇄`{r,g,b}` + colour helpers, `ph_huesort_make` (bilinear corner gradient → target + locked-corner arrays), two-pass date select, `ph_huesort_scramble` (date-seeded interior Fisher-Yates), `ph_huesort_is_solved_arr`, board serialise/restore. Save helpers (`ph_huesort_is_done`/`mark_done`, `ph_huesort_save_state`/`load_state`) live here too (mirrors Shikaku). |
| `scr_arrows` | ~230 | Arrows pure logic: JSON loader/cache + two-pass date select, `ph_arrows_make` (raw→{size, arrows:[{head,cells,len,color_idx}]}), `ph_arrows_delta`, `ph_arrows_sweep_clear` (TIP-lane clear test — straight from cells[0]+dir to edge vs other alive arrows), `ph_arrows_is_solved`, `ph_arrows_first_clear` (longest currently-clear arrow → hint), `ph_arrows_at` (cell→arrow hit-test), state serialise/restore (`save.arrows_state[date]={cleared,penalty,hinted}`, `alive=!cleared`; `hinted` = arrows recoloured green by a hint, persisted so the cue survives a resume), `ph_arrows_is_done`/`mark_done` (`ARROWS`), hardcoded 17×14 fallback. **Non-square 17×14 (rows×cols)** board; `ph_arrows_make`/`ph_arrows_sweep_clear` use `rows`/`cols` (row-major `r*cols+c`); boards reverse-generated solvable + directed gap-filler, ~32–45 arrows/board (several long up to 16 + many short fillers), ~83% fill (`tools/gen_arrows.py`; ~83–87% is the tip-lane construction's practical fill ceiling). |
| `scr_ladder` | ~150 | Ladder (Word Ladder) pure logic: JSON loader/cache + two-pass date select, `ph_ladder_make` (raw→{length,start,words[],clues[],count}), `ph_ladder_diff_pos` (single differing index), `ph_ladder_current_word` (step→shown word: start at step 0, else words[step-1]), hardcoded 4-letter `COLD→…→CORK` fallback. Save helpers (`ph_ladder_is_done`/`mark_done`, `save_state`/`load_state` `{step,hinted,hint_lvl}`) live in `scr_save`. |
| `scr_colordoku` | ~250 | Colordoku (Queens / Meowdoku) pure logic: JSON loader/cache + two-pass date select, `ph_colordoku_make` (raw→{size, regions[], solution[]}), `ph_colordoku_region_color` (6 distinct pastels, avoids the teal gem), `ph_colordoku_adjacent`/`ph_colordoku_pair_conflict` (same row/col/region or touching), `ph_colordoku_conflicts` (per-cell bad-queen bool array → red highlight), `ph_colordoku_is_solved` (==N queens, zero conflicts), `ph_colordoku_forced_x` (blank cells ruled out by a placed queen) + `_has_forced_x` — **2026-06-19 the hint now places only the first `PH_COLORDOKU_HINT_XS` (3) of these as LOCKED X's** (`obj_colordoku` cd_apply_hint; `hint_x_locked[]` blocks the tap-cycle, persisted). State serialise/restore now carries a parallel **`xlock`** mask: `save.colordoku_state[date]={cells, xlock}` (two 36-char strings; `ph_colordoku_save_state(save,date,state,xlock=undefined)` preserves an existing mask when `xlock` omitted, `ph_colordoku_load_xlock` reads it back). `ph_colordoku_is_done`/`mark_done` (`COLORDOKU` flag). Hardcoded unique 6×6 fallback. Counts toward the daily goal like any puzzle (no skip rule). |
| `scr_wordbend` | ~210 | Word Bend pure logic: JSON loader/cache + two-pass date select, `ph_wordbend_make` (rebuilds the letter `grid` by placing each word's chars along its `path`), **5×5** board, fallback 5×5, `ph_wordbend_match` (traced cell-index seq vs each unfound word's path, forward/reverse), `ph_wordbend_is_solved` (all words found), `ph_wordbend_longest_unfound` (hint target), found/hinted index serialise/restore (`save.wordbend_state[date]`), `ph_wordbend_is_done`/`mark_done` (`WORDBEND` flag). |
| `scr_wordle` | ~190 | Wordle pure logic: answer + validation-list loaders/caches, two-pass date select, `ph_wordle_make`, `ph_wordle_score_guess` (green/yellow/gray dup logic), `ph_wordle_is_allowed`, `ph_wordle_add_guess` (win/loss), `ph_wordle_grant_extra_moves`, `ph_wordle_keyboard_states`, guess serialise. Save-struct helpers (is_done/mark/is_missed/state) live in `scr_save`. |
| `scr_missions` | ~330 | **Missions feature engine** (weekly, personal install-anchored window — see `Docs/MISSIONS_PLAN.md`). Catalog of mission defs (`ph_mission_def`/`ph_mission_catalog`, per-puzzle missions generated by looping `ph_mission_puzzles()`); weekly stats (`ph_week_new_stats`, `ph_week_metric` incl. computed `distinct_types`/`types_ge3`); mission queries (`ph_mission_value/progress/title/is_complete/claimable/claim`); week lifecycle (`ph_week_init` hand-authored Week 1 / `ph_week_draw` random week ≥2 with `ph_week_pick_band` cooldown / `ph_week_check_finish` → "finished" / `ph_week_collect` grants stragglers+bonus then `ph_week_advance`). Counter hook `ph_week_record_solve(save, claim_key)` called from `ph_win_grant` + generic `ph_week_bump`. **Phase 1–2 done (data complete).** Phase 2 added the rest of the counters & their hooks: bonus words (`ph_week_record_bonus_word` from `obj_anygram`/`obj_wordbend` Step), no-hint (`ph_week_mark_hint_used` from `scr_hint` paid/freed, keyed by the new 4th `ph_hint_create(...,key)` arg = claim_key; read in the solve hook), speed (`ph_timer_get(save,claim_key) < PH_MISSION_SPEED_SECS`), Wordle ≤3 (guess count from `ph_wordle_load_state`) and win-streak (`wordle_win_run`/`wordle_win_best`, reset via `ph_week_record_wordle_miss` from `obj_wordle wd_finalize_loss`). Catalog now has ~26 authored defs (×10 per-puzzle expansion). STILL DEFERRED: `hat_trick` (needs per-day distinct-type tracking). Added `ph_week_time_left_str` (compact "Xd Yh"/"Xh Ym" reset countdown for the Event Hub header pill). **Phase 3 Event Hub UI is BUILT** (`obj_profile`, see below). **NOT built yet: the Week Complete→Collect designed screen (placeholder only).** |
| `scr_draw` | ~360 | Reusable draw helpers: `ph_draw_rounded/chip/text/text_shadow/icon`, easing, `ph_scissor_gui`, hit tests, `ph_draw_nav` (bottom tab bar), `ph_draw_burst`, cached `ph_draw_dot_bg`. **Shared button widgets** (Penpot Win Screen design): `ph_draw_reward_btn(...)` and `ph_draw_nav_btn(...)` — both now draw an **image background** via `ph_draw_btn_bg(spr,x1,y1,x2,y2)` (horizontal 3-slice) when `ph_btn_sprite_for(body_col)` matches a colour→art mapping (blue/green/pink/red), else fall back to the primitive chip. **Shared puzzle widgets:** `ph_draw_game_tip(grid_top,str)` (one-line objective hint, Nunito-reg 44 faint ink, wraps to 2 lines — see `ph_game_tip` in `scr_constants`); `ph_draw_bonus_pill(l,cy,count)` (white capsule · chest · "BONUS" · pink count badge; returns `{l,r,t,b,icon_x,icon_y}`; used by Anygram + Word Wave); `ph_draw_word_tile(cx,cy,w,h,r,text,found)` (Word Wave "words to find" pill — found = pink+white+strike, to-find = tan+faint ink; `PH_COL_WORD_*`). |
| `scr_fonts` | 19 | `ph_load_fonts()` — registers all `global.fnt_*` via `font_add`. |
| `scr_spin` | ~290 | **Daily Spin prize wheel** (retention hook; modal lives in `obj_hub`, no new object/room). Struct-based like `scr_hint`. `ph_spin_eligible(save)` (session_count ≥ `PH_SPIN_UNLOCK_SESSION` and not yet claimed today), `ph_spin_create()`, `ph_spin_open` (pre-rolls a uniform prize + computes `rot_final` so the chosen slice lands under the top pointer), `ph_spin_is_open`, `ph_spin_tick` (entrance + spin ease-out + result/video phases, runs through the input lock), `ph_spin_input(s,mx,my)` → `"none"/"spun"/"claimed"`, `ph_spin__grant` (coins + `spin_claimed_date` + save). Draw (**2026-06-17 redesign**): light dim + a **cream bottom-sheet** (`PH_COL_YELLOW_SOFT`, slides up — the design's "hint-modal yellow box") over the lower screen with the hub visible above; 6-wedge wheel (`ph_spin__wedge` trianglefan) at the sheet top with **radially-angled prize numbers** (`draw_text_transformed`, rotation folded to [-90,90]) + the coin tucked inward of each number; solid-red `#f43327` pointer; centre SPIN hub; idle "Spin and Win!" title+subtitle and the **CLAIM / DOUBLE** result (dark amount, image-backed buttons) all inside the sheet. `ph_spin__draw_result(_s,_cx,_slide)`. Prizes = `ph_spin_prizes()` (10/25/50/75/100/150). Phases: idle→spinning→result→(video). |
| `scr_tutorial` | ~80 | **Soft finger-pointer hint** (no overlay/text/dimming/input-capture). Redesigned 2026-06-16 from a stepped coachmark *tour* (dim + captions + dots + Skip) that playtested badly (users tried to swipe between steps, felt they "got it wrong") down to a tiny reusable primitive: a bobbing finger (`global.spr_finger` ← `datafiles/icons/finger.png`) that fades in, points at a target, fades out. `ph_finger_create()`, `ph_finger_point_at(f,x,y,ang)` (ang 0 = tip up, hand below target), `ph_finger_hide(f)`, `ph_finger_is_visible(f)`, `ph_finger_tick(f)` (fade+bob), `ph_finger_draw(f)`. Stateless re where/when to show — the HOST decides. **First use: hub first-run onboarding** (`obj_hub` slides the tile list up, then points the finger at card 0's PLAY ~1 s later — fully non-blocking; flag `save.tutorial_done` set on first puzzle open, cleared by `ph_save_reset`). Intended for the planned per-puzzle first-play hints too. (Script already registered in `.yyp` — no new resource.) |
| `scr_notify` | ~55 | **Local daily-reminder notification (iOS).** `ph_notify_supported()` (= `os_type==os_ios`), `ph_notify_boot()` (re-arm on launch if opted in), `ph_notify_request_after_first_solve()` (prompt + schedule once, sets `save.notif_requested`), `ph_notify_cancel()`. Thin GML over the native **`LocalNotifications`** iOS extension (`notif_setup_daily_puzzle` / `notif_cancel_all`). All iOS-guarded so other platforms are unaffected. Schedules a repeating **9:30 AM** "Your daily puzzle is ready!" via `UNCalendarNotificationTrigger`; a tap just opens the app. Hooked from `ph_win_grant` (first solve) + `obj_persistent` Create (boot re-arm). |

### Key economy/save facts to remember
- Level is **derived**, never stored: `level = floor(xp / 500) + 1`. `ph_grant_xp` computes `levels_gained × 100` coins, but the four `*_check_win` now call it with `_auto_coins=false` so the level-up coins are **deferred** to the **Level-Up reward screen** (`obj_win`/`rm_win`). On level gain they set `global.pending_levelup = {level, base_reward:100}`; the screen grants 100, or 200 if the player picks DOUBLE (placeholder video). Only 1 level-up possible per puzzle (100 XP grant vs 500/level).
- `ph_solved_count_on` counts a day's solves but **skips bookkeeping keys** prefixed `ANYGRAM_` and `WW_W` so per-word flags don't inflate the daily count. Adding a new multi-flag puzzle? Add the same skip rule.
- Anygram completion = `ANYGRAM_DONE` (new) or legacy `ANYGRAM_M1 && ANYGRAM_M2`, via `ph_anygram_is_done`. Sudoku = `SUDOKU` key. Word Wave = `WORDWAVE` key.
- Streak recomputed on every save load and after completions via `ph_update_streak`.
- **`save.week`** (Missions) is backfilled in `ph_save_load`/`ph_save_reset` via `ph_week_init`, then `ph_week_check_finish` runs on load. Holds `{index, status("active"|"finished"), start_dt (datetime real), missions[], stats, bonus_claimed, recent}`. Weekly counters advance only through `ph_week_record_solve`, called once per genuine solve inside `ph_win_grant` (never in review mode).
- **Puzzle timer measures active play time, not wall-clock.** Each controller sets `timer_key = "<puzzle>_" + date` and `timer_base_secs = ph_timer_get(...)` in Create, then displays/records `ph_timer_now(timer_base_secs, session_start_ms)`. Step calls `ph_timer_step(...)` while playing (persists ≤ once/sec → kill-safe); the back-button handler calls `ph_timer_commit(...)` + `ph_save_write`. Stored in `save.timers`; backfilled in `ph_save_load`. Wordle gates ticking on `puzzle.status == "in_progress"`. See GDD §5.2.

---

## 4. Objects (screens & controllers) — `objects/`

Each object owns a screen; logic split across `Create_0` (setup/state), `Step_0` (input/update), `Draw_64` (GUI-space render). Puzzle controllers are the big ones.

| Object | Events (LOC) | Role |
|---|---|---|
| `obj_boot` | Create(5) | Spawns `obj_persistent`, jumps to `rm_hub`. |
| `obj_persistent` | Create, **Step**, **Draw**, CleanUp | **Session manager.** Sets dynamic `PH_H`, iOS safe-area insets, loads fonts + save, configures surface/MSAA/filtering, loads **all sprites** via `sprite_add`. Lives whole session. **Screen-transition owner (2026-06-19):** because it spans every room it drives the hub→puzzle **iris transition** — Create inits `global.trans_*` state + `TRANS_COVER_FR/REVEAL_FR` and sets `depth=-100000` (draw on top of any room); **Step** advances the iris (cover phase → `room_goto(global.trans_room)` under full cover → reveal phase → done); **Draw GUI** renders the accent-colour iris (cover = `ph_ease_in` circle grows from the tap point to fill; reveal = `ph_ease_out` circle shrinks back to a point, edges first) + an additive white "click" spark at the origin. Kicked off via `ph_trans_begin(ox,oy,col,room)` (in `scr_draw`). ⚠️ The Step + Draw GUI events are NEW events on an existing object → needs a **GameMaker quit + clean rebuild** (eventList edited in `obj_persistent.yy`: added `eventType:3` and `eventType:8/eventNum:64`). Step also maintains **`global.ph_idle_anchor`** (init in Create, reset to `current_time` on any tap) — the inactivity clock the shared HINT-pill bounce reads (`ph_hint_pill_draw`, 2026-06-19). |
| `obj_hub` | Create(99), Step(175), Draw(401) | Home screen: 7-day strip, collapsible month calendar, scrollable game cards, progress tube. Reads solved-state via `ph_*_is_done`. **Card tiles match the Penpot "Puzzle Tile" 1:1 (2026-06-11):** `card_h=317` keeps the source sprite's 1430×450 (≈3.18:1) aspect at the 1008px render width, so `_card_sx≈_card_sy` and the whole tile scales uniformly. All tile metrics are authored in **source-tile space (1430×450)** in Draw §6 and mapped to screen via `_card_left + sx*_card_sx`: icon 250² (design 320, trimmed per Bora) at left+50 (vert-centred), title (`fnt_disp_lg`, 60) + description (`fnt_body_md`, 36) left-aligned at icon-right+50, both black@0.6, right-clipped to `_pill_left-16` so long text can't slide under the pill. **Right pill** is one uniform white capsule (`ph_draw_pill`, `PH_COL_WHITE` opaque) with its right edge at source-x 1380 (tile-right−50), **dark `PH_COL_DARK` text**; the stopwatch + trophy are **full-colour 3D sprites drawn `c_white` (no tint)** — tinting `icon_stopwatch.png` dark turned it into a black blob. solved/MISSED = colour stopwatch + mm:ss (red if missed), `time_trophy` = colour trophy + time, `locked` = centred "COMING SOON", everything else = centred "PLAY". The old per-variant translucent/dark-chip pills were removed. **Solved-card finish-time pill is centralized** (Draw §badge): any solved puzzle shows its `mm:ss` from `save["<key>_time_<date>"]`, where `<key>` is derived from the card's room (`rm_<key>`) — no per-name branching, so new puzzles get it for free. Wordle's MISSED day is the only exception (time in red via `ph_wordle_is_missed`). The old generic "SOLVED" badge was removed. **Coin-flow reward anim** (Create/Step §coin-flow, Draw §8): when entered straight after a Level-Up claim it streams coins into the top-right coin pill and floats a "+N" label under it; driven by `global.coin_flow_amount` (consumed on Create). The coin-flow init is now a reusable method `hub_start_coinflow(amount)` (called by both the Level-Up entry path and the Daily Spin claim). **Daily Spin (2026-06-16):** Create builds `spin = ph_spin_create()` and opens it if `ph_spin_eligible(global.save)`; Step calls `ph_spin_tick(spin)` then, while open, captures ALL input (`ph_spin_input` → `hub_start_coinflow` on claim) and `exit`s so the hub is suspended; Draw calls `ph_spin_draw(spin)` last (on top). Session count is incremented once per launch in `obj_persistent` Create. **Soft onboarding (2026-06-16, redesigned):** non-blocking first-run hint (no overlay/text/dim). Create seeds `finger = ph_finger_create()` + `intro_*` state, gated on `!save.tutorial_done`, and parks `scroll_y = scroll_max` (bottom = last game). Step END drives an **auto-scroll sweep** last so it overrides manual scroll: `scroll_y = scroll_max*(1-ease_out(intro_t))` from the last game (bottom) up to the first (top) over ~0.8 s; ~1 s after it settles aims the soft finger at card 0's PLAY pill (hides if card 0 scrolls off); sets `tutorial_done`+saves on the first puzzle launch (card-tap nav). Card taps gated off only while `intro_t < 1`. Draw calls `ph_finger_draw(finger)` last. Daily Spin suppressed while `intro_active`. **Tile click + transition (2026-06-19, `puzzle-hub-animation` skill):** a pressed card sinks `CARD_PRESS_DY` px while held (`card_press_idx`/`_t`, `ph_ease_out`) and **spring-pops** back on release (`card_pop_idx`/`_t`, `ph_ease_out_back(…,2.2)`, applied as a y-offset on `_cy1`/`_ccy` in Draw §6); a scroll cancels the press. Opening a card no longer `room_goto`s directly — it calls **`ph_trans_begin(tap_x, tap_y, card.text_col, room)`** (the iris transition owned by `obj_persistent`, see its row) and locks input ~800 ms; the room swaps under full cover. See `scr_tutorial` + GDD §2.11. |
| `obj_anygram` | Create(321), Step(327), Draw(512) | Anygram puzzle: letter wheel, crossword grid, fly-tile anim, hint, bonus modal, win overlay, confetti. Largest controller. |
| `obj_sudoku` | Create(201), Step(166), Draw(267) | 9×9 board, number pad, conflict highlighting, hint, win overlay. |
| `obj_shikaku` | Create, Step, Draw | 9×9 grid, drag-corner-to-corner rectangles, tap-to-delete, shape-glyph hint, win overlay. Blue accent; bottom HUD = coin + hint (no chest), like Sudoku. |
| `obj_wordwave` | Create(221), Step(243), Draw(361) | 8×8 word-search grid, swipe selection, per-word colors, hint, win overlay (centered card). **NEW layout (Penpot):** "words to find" list is a centred 2-col **pill-tile** block (`WL_*` metrics; `ph_draw_word_tile`, pink found / tan to-find) **above** the grid; **`WL_TILE_W` auto-fits the longest word** (measured at `fnt_tip` in Create, clamped min 223 / max = fits 2 cols) so long words don't clip; grid bottom-anchored above the toolbar. Toolbar = shared `ph_draw_bonus_pill` (left, rect hit `BONUS_PILL_*`) · timer · HINT. |
| `obj_wordle` | Create(~330), Step(~110), Draw(~175) | 6×6 board, **custom on-screen keyboard** (slot-based active row so a hint locks a position; tapped key darkens briefly via `key_press_tag`/`key_press_t` for press feedback), staggered reveal, shared hint (`wd_apply_hint`/`wd_can_hint`), win via shared `ph_win_*`. **Loss flow is self-contained here:** `lose_phase` ∈ `none/aversion/confirm/screen` with `wd_lose_step/input/draw`; buy/free +3 moves (board grows to 9 rows, cells shrink), UNLUCKY screen grants 25/50 XP via `wd_lose_claim`. Green accent; HUD = coin top-right, timer+hint bottom bar. |
| `obj_huesort` | Create(~210), Step(~85), Draw(~130) | Hue Sort: 4×4 colour-gradient board, locked corner anchors (pin dot), **drag-and-drop swap** (pick up → lift onto finger → drop to swap), shared hint (`hs_apply_hint` anchors one correct tile), win via shared `ph_win_*`. Violet accent; HUD = coin top-right, timer+hint bottom bar (Shikaku-style, no chest/pad). No loss state. |
| `obj_wordbend` | Create(~210), Step(~115), Draw(~165) | Word Bend: 5×5 letter board fully tiled by hidden words. Tap a word's first letter + drag across adjacent cells (orthogonal bends, backtrack to trim); release matches the traced cell-seq vs unfound word paths (fwd/rev) → locks green. Shared hint (`wb_apply_hint` rings the first letter of the longest not-yet-found/hinted word), win via shared `ph_win_*`. Tangerine accent (`card_tangerine`); HUD = coin top-right, timer+HINT bottom (no bonus). No loss state. |
| `obj_arrows` | Create(~210), Step(~80), Draw(~110) | Arrows: non-square 17×14 (ROWS×COLS) screen-filling white dot-grid board (CELL fits the play band, BOARD_W/BOARD_H, thinner 0.24·CELL ribbons); arrows drawn as slim ribbons (`RIBBON_W≈0.30·CELL`) with corner-rounded bends (`ar_round`, `AR_CORNER≈0.42`) + triangle arrowhead, per-arrow colour via `ph_colorlink_color`. Tap (`ar_cell_at`→`ph_arrows_at`) → if `ph_arrows_sweep_clear`: snake slide-out (`ar_start_launch` builds smoothed body+lane path, body glides by arc-length, clipped to board) then `alive=false`/save/win-check. **Slide-out is 3-phase juiced (2026-06-19, per the `puzzle-hub-animation` skill):** a brief **recoil** wind-up (head pulls back `LAUNCH_COIL` cells over `LAUNCH_WIND_FRAC` of the launch), then it fires off **accelerating** (`ph_ease_in_cubic`, replacing the old `power(launch_t,1.4)`) with a **motion-smear** ribbon-thinning (`LAUNCH_STRETCH`), and an **exit flash** reaction (additive coloured glow + white core at `launch_exit_x/y`, fired when the head crosses `launch_edge_s`, drawn unclipped after `ph_scissor_reset`). Knobs `LAUNCH_*` in Create; `launch_frames` bumped to 18–34 to fit the wind-up; else blocked → **snake-glide head-first up to the blocking arrow and back** (`ar_start_bump` reuses the launch path machinery via shared `ar_path_at_on`, building body+exit-lane only as far as the blocker; `ph_arrows_block_info` → clear-cell gap + blocker idx; `bump_idx`/`bump_t`/`bump_path`/`bump_arc`/`bump_body_len`/`bump_head_s0`/`bump_reach`/`blocker_idx`, drawn as a partial reversible snake in Draw, reach scales with gap) with **both arrows flashing red (~0.4 s)** + `penalty_secs`+=5 + `timer_base_secs`+=5 + floating "+5 s" (pure visual, input not locked). Shared hint (`ar_apply_hint` recolours the longest safe unhinted arrow **green permanently** via `ar_hinted[]` + soft static halo, persisted in save — replaced the old fading white pulse players kept losing; `ar_hint_pick` skips already-hinted). Win via shared `ph_win_*`; recap = initial full board. Silver accent; HUD = coin top-right, timer+HINT bottom (no bonus). No loss state. |
| `obj_colorlink` | Create, Step, Draw | Color Link (Flow Free): tall non-square **9×7 (ROWS×COLS)** screen-filling board (CELL fits the play band like Arrows, BOARD_W/BOARD_H, row-major `r*COLS+c`), `route[]` per flow (cell-index arrays) + `cell_owner[]`, drag to draw/extend/retract flows (`cl_try_step` walks head toward finger; override trims crossed flows; hint-locked flows immovable), shared hint (`cl_apply_hint` lays the longest solution flow + locks it), win via shared `ph_win_*`. Lime accent; HUD = coin top-right, timer+HINT bottom (no bonus). No loss state. |
| `obj_ladder` | Create(~210), Step(~110), Draw(~115) | Ladder: single word row of N tiles (N = day's word length, sized to fit), tap a tile → `sel` (amber `#ffc04c` bg), type a key → replace that letter, then compare full word vs `puzzle.words[step]`; match → green flash (`#aaca31`) `FB_DUR` frames then `ld_advance` (step++, load next word/clue, or win); mismatch → red flash (`#eb5a5a`) then revert. mismatch also costs **+5 s** (`PH_LADDER_PENALTY_SECS`, folded into `timer_base_secs`) with a red timer flash + floating "+5s" (`pen_t`). Letters-only keyboard (`ld_build_keys`, no DEL/SEND) with **press feedback** (tapped key darkens for `KEY_PRESS_DUR`). Clue box (`#f1eae1`) + `N/10` progress above the keyboard. Shared hint is **two levels per rung** (`hint_lvl`): 1st highlights the differing tile bg (soft amber `#ffe5a8`), 2nd highlights the correct letter on the keyboard (amber `hint_key`); modal subtitle swaps via `ld_hint_subtitle`; both persist per rung. Win via shared `ph_win_*` (recap = full solved ladder, `win_draw_recap`); 100 XP granted by the win-screen CLAIM (`claim_key="ladder_<date>"`), NOT the controller. Amber accent; HUD = coin top-right, timer+HINT bottom. No loss state. |
| `obj_colordoku` | Create(~190), Step(~80), Draw(~95) | Colordoku (Queens): 6×6 region-coloured board (CELL=160, Hue-Sort geometry/bottom-anchor). Single tap cycles a cell empty→X→queen→empty (`state[]` 0 empty·1 X·2 queen; `(state+1) mod 3` — no double-tap/timing); teal **gem** via `cd_draw_gem` (faceted diamond w/ white rim, drawn in code). `cd_draw_cell` colours each tile via region id; conflicting queens (`ph_colordoku_conflicts`) flash red-orange w/ dark gem (no block, Sudoku-style). Shared hint (`cd_apply_hint` fills ALL forced X's; `cd_can_hint`=`ph_colordoku_has_forced_x`), shared win via `ph_win_*` (recap = solution gems). Bright-teal accent; HUD = coin top-right, timer+HINT bottom (no bonus). No loss state. |
| `obj_shop` | Create(2), Step(18), Draw(24) | Shop tab — minimal/stub. |
| `obj_profile` | Create, Step, Draw | **Event Hub screen** (Penpot "Events" design, 2026-06-16 redesign — was "Profile Screen"). **Dedicated to weekly missions: the player identity card + mint MISSIONS panel are GONE.** Top bar (level ★ pill · **EVENT HUB** title · coin pill) on a **cream band**; body is **teal `#adfff1`**. Header = centred description "Complete missions to win extra rewards" + a white **reset-timer pill** (stopwatch + "Xd Yh" via `ph_week_time_left_str`, new in `scr_missions`). Below: a **drag-scrollable mission list** (cull + `ph_scissor_gui`), **sorted claimable → in-progress → claimed** via `prof_sorted_indices` (Draw + Step iterate the same order). Card background is the **`card_mission` sprite** (`global.spr_card_mission`, 1410×390 — cream rect + divider + reward column baked in; `CARD_H` tracks its 1410:390 aspect ≈279, drawn `c_white`, claimed tinted `(232,228,224)`); the white icon tile + puzzle icon are drawn on top. Titles in `fnt_tip`; 3 states: **In Progress** (title top + purple fill bar with "x / N" to the RIGHT + reward ★ amount), **Completed/claimable** (blue **CLAIM** via `ph_draw_reward_btn` on top + **description below the button** + reward ★), **Claimed** (tinted card, faded vertically-centred title, **right column = the `Checkmark` sprite** `global.spr_checkmark` (194×172 gold-outlined purple tick) replacing the star/amount). Both PNGs live in `datafiles/icons` and are registered IncludedFiles. Geometry consts (`CARD_*`, `DIVIDER_X`, `ICON_*`, `MAIN_X*`, `REW_CX`) + `prof_metrics`(header: `topbar_cy`/`band_bot`/`desc_y`/`timer_cy`/`list_top`/`list_bot`)/`prof_card_top`/`prof_claim_rect`/`prof_icon`/`prof_sorted_indices` in Create. CLAIM → `ph_mission_claim`; success plays a **collision-synced claim celebration** (polished 2026-06-18) driven by a 2-phase state machine (`claim_phase` 0-idle/1-STARFLY/2-REORDER, `claim_t`, `claim_mi`): **(1) STARFLY** (Royal-Match-style, reworked 2026-06-18) — the claimed tile's reward ★ **DUPLICATES** into `STARFLY_N` copies that **appear ONE BY ONE** (`SF_SPAWNGAP` apart — #0 is the original ★, the rest pop in with an `ph_ease_out_back`) and **gather + mill around each other** at the spot as a **loose organic cluster** (each spirals out over `SF_GATHER`; per-copy deterministic-random radius/speed/phase + a slow bob off `SF_ORBIT_R`/`SF_ORBIT_SPD` so they don't sit on a perfect ring; squashed 0.62 for a pseudo-3D feel; swirl for `SF_ORBIT`), then **peel off ONE BY ONE** (`SF_RELGAP` apart) and **fly up ACCELERATING** (cubic ease-in `ph_ease_in_cubic`, a gentle Bézier bow) over `SF_TRAVEL`, **vanishing at the level ★ with an additive light flash** (`SF_FLASH`). Drawn solid (no fade-in); each copy grows to the HUD star's size (110/512) on arrival. The level ★ **embraces** the stream: `levelstar_t` triggers `LEVELSTAR_LEAD` frames before the FIRST arrival, **EXPANDS** open (`ph_ease_out` grow by `LEVELSTAR_EMBRACE`), **HOLDS** open with a small squash pop on each arrival (every `SF_RELGAP`), then **absorbs** (cosine dip) + settles over `LEVELSTAR_TAIL`. The tile's **★→checkmark pop** fires when the **LAST copy leaves the spot** (`SF_GATHER+SF_ORBIT+(STARFLY_N-1)*SF_RELGAP`, `ph_ease_out_back(…,2.4)` over `CHECK_POP`) — the last orbiting copy covers it until it flies, so it's revealed seamlessly. Phase 1 ends after the last copy lands + the ★ settles. **(2) REORDER** — starts the instant the checkmark lands: the claimed tile first does an in-place **anticipation BOUNCE** (`BOUNCE_PX` up-and-back over `REORDER_BOUNCE` frames, list held still), then everyone **slides** from `slot_old[]` to their new sorted slot via smoothstep + a capped `REORDER_OVERSHOOT_PX` settle, with the **claimed tile drawn FIRST so it sinks BEHIND the remaining tiles** as it descends (`_draw_order` puts `claim_mi` first; `_pos_of[]` decouples target slot from draw order). During phase 1 the list is **held at `slot_old` positions** (the claiming tile renders a special transition — title centred, fading amount, then the popping checkmark — via the `_claiming` flag, un-tinted); the flying-star block recomputes its source from the claimed tile's live position each frame. Claim taps gated on `claim_phase == 0`. A level-crossing skips the celebration and routes via `ph_win_route(rm_profile, …)`. Beat lengths are independent instance consts in Create (`STARFLY_N`, `SF_GATHER/_SPAWNGAP/_ORBIT/_RELGAP/_TRAVEL/_ORBIT_R/_ORBIT_SPD/_FLASH`, `CHECK_POP`, `LEVELSTAR_LEAD/_TAIL/_EMBRACE`, `REORDER_BOUNCE`, `REORDER_DUR`, `BOUNCE_PX`, `REORDER_OVERSHOOT_PX`). **Every tween is eased for dynamic accel/decel** (no linear pace): copies orbit then **fly accelerating** (`ph_ease_in_cubic`) and flash on arrival (additive), checkmark `ph_ease_out_back(…,2.4)` (poppy overshoot), level-★ embrace = eased grow + hold-with-pulse + cosine absorb/settle, reorder slide `ph_ease_in_out` (cubic), and the claimed-tile bounce is asymmetric (ease-out up to a hang, ease-in back down). Easing helpers (`ph_ease_in/in_cubic/out/out_cubic/in_out/back/out_back`) live in `scr_draw`. Finished week → minimal **WEEK COMPLETE / COLLECT** placeholder calling `ph_week_collect` (Phase 4 replaces with the designed screen). Still has the **hidden triple-tap level-pill save-reset** gesture. **Nav tab label renamed "Profile"→"Events"** in `ph_draw_nav` (`scr_draw`); claimable badge still drawn there (all tabs). Object/room internal names stay `obj_profile`/`rm_profile`. |
| `obj_win` | Create, Step, Draw | **Level-Up reward screen** (in `rm_win`). Repurposed from the old dead win-overlay stub. Reads `global.pending_levelup`, shows a violet screen ("Congratulations" + level # + "LEVEL UP!" + 3D star + confetti), then "Claim your reward!" + a **reward amount** ("100 🪙", `fnt_num_xl` + gold coin) and **CLAIM | DOUBLE** word-label buttons (`ph_draw_reward_btn`, TV badge on DOUBLE) — 2026-06-14 redesign; grants coins (`lu_claim`), clears the flag. **`lu_claim` routing:** if `global.post_levelup` is set (player came from a win-screen NEXT GAME / YESTERDAY shortcut) it continues to that `{room,date}` with **no** coin animation; otherwise it stashes the amount in `global.coin_flow_amount` and `room_goto(rm_hub)` so the hub plays its coin-flow animation. DOUBLE → `ph_video_overlay` 5 s → 200 coins. NB: its video flag is `vid_open` because `video_open` is a reserved built-in. |

Pattern: each puzzle controller embeds its own **win overlay** + confetti (not `obj_win`); review mode re-enters the puzzle room with a `global.*_review_mode` flag and jumps straight to the win overlay. The **Level-Up screen** is the one thing that lives in its own room: each puzzle's win-screen BACK button does `room_goto(ph_levelup_pending() ? rm_win : rm_hub)`.

**Shared hint flow (all 4 puzzles):** each controller's Create builds `hint = ph_hint_create(<x>_apply_hint, <accent>)` and defines `<x>_apply_hint` / `<x>_can_hint` (`ag_`/`sd_`/`ww_`/`sk_` prefixes). Step calls `ph_hint_tick(hint)` near the timers and `ph_hint_input(hint)` before normal input (exit if result ≠ `"none"`); the HINT-pill handler gates on `<x>_can_hint()` then calls `ph_hint_open(hint)`. Draw calls `ph_hint_draw_feedback/modal/video(hint)`. See `scr_hint` + GDD §2.5–2.6.

---

## 5. Rooms — `rooms/`

`rm_boot` (first, per RoomOrderNodes) → `rm_hub`. Then one room per screen: `rm_anygram`, `rm_sudoku`, `rm_wordwave`, `rm_shikaku`, `rm_wordle`, `rm_huesort`, `rm_colorlink`, `rm_wordbend`, `rm_arrows`, `rm_ladder`, `rm_colordoku`, `rm_shop`, `rm_profile`, and `rm_win` (= the **Level-Up reward screen**, no longer unused). Each room hosts its matching object.

---

## 6. Global state surface

Set up in `obj_persistent/Create_0`. Most-referenced globals:

**Data / session**
- `global.save` — the entire save struct (89 refs). All progression lives here.
- `global.selected_date_key` — which day the player is viewing/playing (47 refs).
- `global.input_locked_until` — frame-time input lock during animations (20 refs).
- `global.pending_levelup` — `{level, base_reward}` when a completed puzzle queued a level-up reward; `undefined` otherwise (init in persistent, set in `*_check_win`, consumed/cleared by `obj_win`). Gate via `ph_levelup_pending()`.
- `global.coin_flow_amount` — coins granted by the last Level-Up claim, awaiting the hub coin-flow animation. `0` = nothing to play (init in persistent, set in `obj_win.lu_claim`, consumed in `obj_hub.Create_0`).
- `global.post_levelup` — `{kind:"room", room, date}` when the player triggered a win-screen **NEXT GAME / YESTERDAY** shortcut while a level-up was pending; the Level-Up screen continues here after claiming (no coin anim) instead of going to the hub. `undefined` otherwise (init in persistent, set in `ph_win_route`, consumed in `obj_win.lu_claim`).
- `global.PH_H_dyn` — runtime canvas height (`PH_H` macro reads this).
- `global.safe_top_gui` / `global.safe_bottom_gui` — iOS notch / home-indicator insets.

**Puzzle caches** (lazy-loaded, may be `undefined` sentinel if file missing)
- `global.ph_anygram_cache`, `global.ph_sudoku_cache`, `global.ph_wordwave_cache`, `global.ph_shikaku_cache`, `global.ph_wordle_cache` (answers), `global.ph_wordle_allowed` (validation list as a `{WORD:true}` map), `global.ph_huesort_cache`, `global.ph_colorlink_cache`, `global.ph_wordbend_cache`, `global.ph_arrows_cache`, `global.ph_ladder_cache`, `global.ph_colordoku_cache`.

**Review-mode flags** (jump straight to win overlay; set on hub tap)
- `global.anygram_review_mode`, `global.sudoku_review_mode`, `global.wordwave_review_mode`, `global.shikaku_review_mode`, `global.wordle_review_mode`, `global.huesort_review_mode`, `global.colorlink_review_mode`, `global.wordbend_review_mode`, `global.arrows_review_mode`, `global.ladder_review_mode`, `global.colordoku_review_mode`. (Wordle: a *missed* day isn't "done", so its review flag stays false — `obj_wordle` Create reopens the lose screen from the persisted `status`/`WORDLE_MISSED` instead.)

**Transient anim**
- `global.fly_tiles` — shared fly-tile particle list (22 refs, Anygram).
- `global.ph_dot_surface` — cached dotted-background surface.

**Assets** — `global.fnt_*` (fonts), `global.spr_*` (all sprites). Loaded once in persistent. Includes `global.spr_tv` (`retro tv icon.png`) for the FREE / DOUBLE rewarded-video buttons.

---

## 7. Data file formats (authoring)

- **Anygram** (`puzzles_anygram.json`): array. New shape `{letters:[5], words:[{text,row,col,dir("H"|"V")}], bonus|bonus_pool:[...], date?, grid_size?}`. Legacy 2-word shape still accepted. Date selection: exact `date` match wins, else `seed mod length`.
- **Sudoku** (`puzzles_sudoku.json`): array of `{date?, difficulty?, givens:"81 chars (0=blank)", solution:"81 chars"}`, row-major.
- **Word Wave** (`puzzles_wordwave.json`): array of `{date?, grid:[8×"8 chars"], words:[{text,row,col,dir}], bonus_pool:[...]}`. `dir` ∈ H/H_REV/V/V_REV/DR/DL/UR/UL. Each word and bonus word must trace a straight line on the grid.
- **Shikaku** (`puzzles_shikaku.json`): array of `{date?, size, rects:[{r,c,w,h,cr,cc}]}` (rects are both clue source and unique solution).
- **Wordle** (`puzzles_wordle.json`): array of `{date?, answer:"<6 uppercase letters>"}`, two-pass date selection, fallback `STREAM`. Plus **`wordle_allowed.json`**: a flat array of uppercase 6-letter validation strings (answers should be a subset). Both registered as `IncludedFiles` in the `.yyp`.
- **Color Link** (`puzzles_colorlink.json`): array of `{date?, rows, cols, flows:[{color, a:[r,c], b:[r,c], path:[[r,c]...]}]}` (non-square 9×7; legacy square `size` still accepted by `ph_colorlink_make`). `a`/`b` = endpoint dots; `path` = full solution route (powers the hint + win recap). Generated via Hamiltonian-path-cut over ROWS×COLS so flows always tile the board, 6–7 flows/board, 4–13 cells each (`tools/gen_colorlink.py`; fewer/shorter flows + smaller grid = easier solve). Two-pass date select; hardcoded 9×7 fallback.
- **Hue Sort** (`puzzles_huesort.json`): array of `{date?, size?, corners:{tl,tr,bl,br}}` where each corner is `"RRGGBB"` hex. The four corners define the whole board via bilinear interpolation; the interior scramble is date-seeded. Two-pass date selection; missing file → hardcoded pink/yellow/purple/teal fallback. **2026-06-15: now generated by `tools/gen_huesort.py` (60 boards = 30 past + 30 future); corners placed ~90° apart on the hue wheel at high saturation, rejecting any board whose min adjacent-tile CIELAB dE76 < 18 (the "moderate" difficulty floor). Replaced the old 9-board hand-authored set whose hardest steps fell to dE76 ≈ 1.6–5.6 (near-invisible). Difficulty = the smallest adjacent-tile colour gap, NOT tile count — keep corners distant/distinct-hue. Data-only change (already an IncludedFile; no `.yyp` edit / rebuild).**
- **Word Bend** (`puzzles_wordbend.json`): array of `{date?, size, words:[{text, path:[[r,c]...]}]}`. `path` lists each word's cells in spelling order (`path[k]`↔`text[k]`); the union of all word paths must tile the whole `size×size` board exactly once (no gaps/overlaps) so it's always solvable. The letter grid is rebuilt from words+paths. Two-pass date select; hardcoded 5×5 fallback. Shipped pool 60 boards (`size` 5, 30 past + 30 future dated, ~5 words/board), dictionary-validated words, snakier S-biased geometry (`tools/gen_wordbend.py`, `N=5`). **Bonus words (2026-06-14):** tracing a real ≥4-letter non-hidden word pays +10 coins (`PH_BONUS_WORD_COINS`, coins only) via **`wordbend_dict.json`** — a curated ~24.7k-word common-word set (`wordfreq` freq ≥ zipf 2.6 ∩ web2/gcide, proper nouns + profanity screened), loaded once into `global.ph_wordbend_dict` ({WORD:true} map) by `ph_wordbend_load_dict`. Registered as an `IncludedFile` in the `.yyp`. Found bonus words persist in `save.wordbend_state[date].bonus`; UI = shared BONUS chest-pill (bottom-left) + words-list modal, mirroring Anygram.
- **Arrows** (`puzzles_arrows.json`): array of `{date?, rows, cols, arrows:[{head:"U"|"D"|"L"|"R", cells:[[r,c]...]}]}` (non-square; legacy square `size` still accepted by `ph_arrows_make`). `cells[0]` is the arrowhead (tip) cell; `cells[1]` is directly behind it (opposite `head`) so the tip is frontmost; cells are 4-connected. Generated offline by `tools/gen_arrows.py` via reverse construction (place each arrow only when its tip-lane is clear of placed cells → forward solution = reverse placement order), greedy-solver verified. A directed gap-filler then drops 2-cell arrows into every coverable empty cell. Two-pass date select; hardcoded 17×14 fallback. **17×14**, ~83% fill, ~32–45 arrows/board, several long (up to 16) + many short fillers. Tip-lane escape rule = only the straight lane in front of the tip must be clear.

- **Ladder** (`puzzles_ladder.json`): array of `{date?, length, start, steps:[{word, clue}]×10}`. `start` = seed word (shown pre-filled, not counted in N/10); each `steps[i].word` is `length` letters and consecutive words (incl. `start`→`steps[0]`) differ by exactly one position (validated at authoring). `clue` = JSON-fed description of that rung's word. Two-pass date select; hardcoded 4-letter `COLD→…→CORK` fallback. Current word derives from `step` (not stored).

- **Colordoku** (`puzzles_colordoku.json`): array of `{date?, size, regions:[N*N ints 0..N-1 row-major], solution:[N*N ints 0/1 row-major]}` (6×6). `regions[i]` = colour-region id; `solution[i]`=1 marks the unique solution's queen (powers the win recap). Generated by `tools/gen_colordoku.py` (no-touch queen permutation → contiguous region growth around the queens → backtracking solver confirms a unique solution before shipping). Two-pass date select; hardcoded unique 6×6 fallback. Pool = 60 boards (30 past + 30 future). Rules: one queen per row/col/region, no two queens touching incl. diagonally (LinkedIn-Queens *touch* rule).

All: missing file → hardcoded fallback puzzle. Same two-pass date selection.

---

## 8. Conventions & gotchas

- **No struct literals with non-constant fields.** GameMaker's YYC compiler generates anonymous C++ constructors for non-constant expressions inside struct literals, which can cause **linker errors**. Build such structs with explicit property assignment (see `obj_hub/Create_0` `LAYOUT` and `hub_center_strip_on`). Related: adding resources can trigger a YYC link failure fixed by a **Clean rebuild**, not code changes (auto-memory `project_puzzle_hub_yyc_clean_rebuild`).
- **All gameplay numbers are `#macro`s in `scr_constants`.** Change them there and reflect in `GDD.md`. Never hardcode economy values.
- **Watch for reserved built-in names.** GameMaker ships functions like `video_open`, `video_close`, etc. Naming an instance variable `video_open` fails to compile ("read-only function"). The Level-Up screen uses `vid_open` for this reason. When in doubt, prefix puzzle-specific state.
- **Adding events to an existing object** (e.g. the Create/Step added to `obj_win`) means editing its `.yy` `eventList` (eventType: 0=Create, 3=Step, 8 + eventNum 64=Draw GUI) **and** adding the matching `*.gml` file. Like adding resources, this can need a **Clean rebuild**.
- **`PH_SUDOKU_TEST_PREFILL = true`** in `scr_constants` — debug flag that starts Sudoku ~90% solved. **Must be `false` before shipping.**
- **Local notifications (iOS).** "Your daily spin is ready!" reminder via the native **`LocalNotifications`** extension (`extensions/LocalNotifications/`, `iOSSource/*.mm` uses `UNUserNotificationCenter`); GML wrapper = `scr_notify`. **2026-06-17: the reminder is now SYNCED to the Daily Spin cooldown** (was a fixed daily 9:30 + a `PH_NOTIF_TEST_MODE` 60 s flag — both removed). The native `notif_setup_daily_puzzle(seconds)` now takes a delay: `>0` = one-shot that many seconds out (synced to the spin), `==0` = repeating daily 9:30, `<0` = schedule nothing (spin already available). `ph_notify_spin_delay_secs()` computes it from `PH_SPIN_TEST_COOLDOWN_MINS` + `save.spin_claimed_dt` so it lands exactly when the next free spin unlocks; in production (`PH_SPIN_TEST_COOLDOWN_MINS == 0`) it falls back to the daily 9:30 reminder. Armed via `ph_notify_sync_spin()` — called on boot (`ph_notify_boot`), after the first solve (`ph_notify_request_after_first_solve`, which also shows the one-time permission prompt), and on every **spin claim** (`ph_spin__grant`). Copy is the `PH_NOTIF_BODY` `#define` in `LocalNotifications.mm`. ⚠️ Changing the function's arg count (`argCount`/`args` in `LocalNotifications.yy`) is a native-extension change — needs the extension reimported/registered with GameMaker quit + **YYC Clean rebuild**. Opt-in flag = `save.notif_requested`; cooldown timestamp = `save.spin_claimed_dt`. **Two manual steps it needs:** (1) like any new resource, register it only while GameMaker is **fully quit**, then reopen + **YYC Clean rebuild** (mirrors the iOSSafeArea/`.yyp`-clobber + clean-rebuild memories); (2) **UserNotifications.framework** must be linked — autolinking did NOT work (build failed with `Undefined symbol _OBJC_CLASS_$_UNMutableNotificationContent`), so it's now declared in the extension's `iosSystemFrameworkEntries` in `LocalNotifications.yy`. If that disk edit ever gets clobbered, re-add **UserNotifications.framework** via the extension's iOS *System Frameworks* in the IDE. Local (not remote/push) notifications need **no** special Info.plist keys or entitlements. To add Android later, add a parallel native module behind an `os_android` branch in `scr_notify`.
- **`PH_BONUS_WORD_XP = 25`** is dead (bonus words pay coins only, no XP). Safe to delete.
- **Safe areas.** iOS insets are computed once in `obj_persistent` into `global.safe_top_gui` / `global.safe_bottom_gui` (GUI units). **GameMaker has no working GML call for this** — `os_get_info()` does *not* expose safe-area keys (the old `ios_safe_area_*` read always returned 0, so the UI sat under the Dynamic Island). Current logic: call the **"iOS Safe Area" native extension** (`iOS_get_safe_area()` → JSON → `top`/`bottom` px → GUI units) for true per-device insets, else estimate from screen aspect for tall iPhones (`PH_H*0.075` top, `PH_H*0.042` bottom — calibrated to iPhone 16 Pro). **The extension IS installed** (`extensions/iOSSafeArea/`) and verified working on iPhone 16 Pro (`src=extension`, raw 186/102 px → 167/91 GUI). **It must stay imported or the project won't compile** (the `iOS_get_safe_area()` call is iOS-only, guarded by `os_type == os_ios`); delete that block in `obj_persistent` to revert to the estimate. `obj_persistent` records `global.safe_src`/`safe_raw_top`/`safe_raw_bottom` and logs `[safe-area]` at boot; `PH_DEBUG_SAFEAREA` (in `scr_constants`) toggles an on-hub readout. **Extension gotcha:** `iOSSource/iOSSafeArea.h` needs `#import <UIKit/UIKit.h>` (declares NSObject/CGFloat/NSString) or the iOS build fails with *"Class defined without specifying a base class"*; re-importing the `.yymp` would overwrite that fix. For full-screen layouts use the helpers `ph_safe_top()` / `ph_safe_bottom()` (in `scr_draw`), which add comfort padding (`PH_PAD_TOP` / `PH_PAD_BOTTOM` in `scr_constants`) **on top of** the raw inset so content never crowds the Dynamic Island / status bar (top) or home indicator (bottom) — and still has margin on devices/sims that report a 0 inset. The shared win screen (`ph_win_draw`) lays its vertical flow between these. Puzzle HUDs use `95 + safe_top_gui` (top) and `PH_H - 110 - safe_bottom_gui` (bottom); the hub uses `LAYOUT.*_y += safe_top_gui` and the nav extends by `safe_bottom_gui`; shop/profile headers add `safe_top_gui`. When adding a new screen, anchor top/bottom elements to these, not to raw pixels.
- **Core-game content is bottom-anchored.** Every puzzle's `grid_y` is set to a top value, then shifted DOWN so the whole play cluster (board + any number pad / keyboard / word-list / wheel) sits just above the bottom HUD toolbar, leaving the empty band under the top HUD instead. Pattern in each Create: `grid_y += max(0, (PH_H - safe_bottom_gui - 155 - PH_PLAY_BOTTOM_GAP) - (grid_y + cluster_h))`, where `cluster_h` spans grid_y to the lowest element's bottom (sub-elements like `NUM_Y`/`DEL_Y`/`KB_TOP`/word-list/`_list_y0` all derive from `grid_y`, so they follow). Anygram is the exception — its grid bottom-anchors above the (independent, proportional) wheel: `grid_y = max(170+safe_top, wheel_top - 130 - grid_h)`. Tune the gap via `PH_PLAY_BOTTOM_GAP` in `scr_constants`. Hit tests read `grid_y`/derived vars, so they stay aligned.
- **Drawing is GUI-space** (`Draw_64` / GUI events), sized to `PH_W × PH_H_dyn` via `display_set_gui_size`. Use `ph_scissor_gui` (converts GUI→window px) for clipping, not raw `gpu_set_scissor`.
- **Save path at runtime:** `working_directory + "puzzlehub_save.json"`. When run from the IDE on macOS it lands at `~/Library/Application Support/com.yoyogames.macyoyorunner/puzzlehub_save.json` (auto-memory `project_puzzle_hub_save_path`).
- **Save is forward-compatible:** `ph_save_load` backfills missing fields. Add new fields with a default backfill there.
- **Desktop vs mobile canvas:** mobile derives height from real screen ratio; desktop forces 1920 and letterboxes (see `obj_persistent/Create_0` comment).
- Sprites are mostly white-on-transparent and **tinted at draw time** via `ph_draw_icon`; full-color icons drawn with `c_white`.

---

## 9. Where to make common changes

| Task | Go to |
|---|---|
| Tune XP/coins/hint cost | `scr_constants` (macros) + update `GDD.md` |
| New puzzle solved-tracking | `scr_save` (`ph_mark_*` / `ph_*_is_done`, add skip rule in `ph_solved_count_on`) |
| Add/author puzzles | `datafiles/puzzles_*.json` |
| Hub layout / calendar / cards | `obj_hub` (`LAYOUT` struct in Create) |
| Shared UI widgets (chips, nav, text) | `scr_draw` |
| Fonts / sprites | `scr_fonts` / `obj_persistent/Create_0` |
| Win screen / confetti | inside each puzzle controller's Draw/Step (not `obj_win`) |
| Hint modal / rewarded-video flow | `scr_hint` (shared); per-puzzle `<x>_apply_hint`/`<x>_can_hint` in each Create |
| Level-Up reward screen | `obj_win` (Create/Step/Draw) + `ph_grant_xp(..., false)` in `*_check_win` |
| New screen | new `obj_*` + `rm_*`, register in `.yyp`, wire nav in Step events |

---

## 10. Repo / versioning

Private repo `github.com/bhogha/Puzzle-Hub`. Tag `v0.1` = rollback baseline; current dev = `0.2` (auto-memory `project_puzzle_hub_github`, `project_puzzle_hub_version`).
